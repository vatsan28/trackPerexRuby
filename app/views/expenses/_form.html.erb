<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_field :reason, id: :expense_reason %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :expense_amount %>
  </div>

  <div class="field">
    <%= form.label :expense_id %>
    <%= form.text_field :expense_id, id: :expense_expense_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
